import { partOneSolution, partTwoSolution } from './Day2';

const bagInput = { red: 12, blue: 14, green: 13}

function runner() {
  console.log("Runner started....");
  // partOneSolution(bagInput);
  partTwoSolution();
}

runner();